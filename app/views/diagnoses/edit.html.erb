<% content_for(:title) { "#{@diagnosis.title} - 編集" } %>

<div class="space-y-6">
  <div>
    <h2 class="text-2xl font-bold text-gray-800">財産診断編集</h2>
    <p class="text-gray-500 mt-1">
      <%= link_to @customer.name, customer_path(@customer), class: 'text-blue-600 hover:underline' %> /
      <%= @diagnosis.title %>
    </p>
  </div>

  <%= render 'form' %>
</div>

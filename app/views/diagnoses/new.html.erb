<% content_for(:title) { "#{@customer.name} - 財産診断アップロード" } %>

<div class="space-y-6">
  <div>
    <h2 class="text-2xl font-bold text-gray-800">財産診断アップロード</h2>
    <p class="text-gray-500 mt-1">
      <%= link_to @customer.name, customer_path(@customer), class: 'text-blue-600 hover:underline' %> の財産診断を登録
    </p>
  </div>

  <%= render 'form' %>
</div>

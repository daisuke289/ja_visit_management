<% content_for(:title) { '訪問種別を編集' } %>

<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <h2 class="text-2xl font-bold text-gray-800">訪問種別を編集</h2>
    <p class="text-gray-500 mt-1"><%= @visit_type.name %></p>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <%= render 'form' %>
  </div>

  <% if @visit_type.usage_count > 0 %>
    <div class="mt-4 bg-yellow-50 border border-yellow-200 text-yellow-800 px-4 py-3 rounded">
      <p class="text-sm">
        この訪問種別は <%= @visit_type.usage_count %> 件の訪問記録で使用されています。
        名前を変更すると過去の記録にも影響します。
      </p>
    </div>
  <% end %>
</div>

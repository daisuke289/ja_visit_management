<% content_for(:title) { "#{@customer.name} - 家族を追加" } %>

<div class="space-y-6">
  <div>
    <h2 class="text-2xl font-bold text-gray-800">家族メンバー登録</h2>
    <p class="text-gray-500 mt-1">
      <%= link_to @customer.name, customer_path(@customer), class: 'text-blue-600 hover:underline' %> の家族を追加
    </p>
  </div>

  <%= render 'form' %>
</div>

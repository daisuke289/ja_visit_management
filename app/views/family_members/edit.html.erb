<% content_for(:title) { "#{@family_member.name} - 編集" } %>

<div class="space-y-6">
  <div>
    <h2 class="text-2xl font-bold text-gray-800">家族メンバー編集</h2>
    <p class="text-gray-500 mt-1">
      <%= link_to @customer.name, customer_path(@customer), class: 'text-blue-600 hover:underline' %> /
      <%= @family_member.name %>
    </p>
  </div>

  <%= render 'form' %>
</div>

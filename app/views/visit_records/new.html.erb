<% content_for(:title) { '訪問記録を登録' } %>

<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <h2 class="text-2xl font-bold text-gray-800">訪問記録を登録</h2>
    <p class="text-gray-500 mt-1">
      <%= link_to @customer.name, customer_path(@customer), class: 'text-blue-600 hover:underline' %>
    </p>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <%= render 'form' %>
  </div>

  <div class="mt-4 bg-yellow-50 border border-yellow-200 text-yellow-800 px-4 py-3 rounded">
    <p class="text-sm">
      <span class="font-medium">60秒入力を目指しましょう！</span>
      折衝内容は50文字以上必要です。重要なポイントを簡潔に記録してください。
    </p>
  </div>
</div>
